version: "2"
sql:
  - schema: "db/schema.sql"
    queries: "db/queries/"
    engine: "sqlite"
    gen:
      go:
        package: "db"
        out: "db/sqlc"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
